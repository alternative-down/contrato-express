export const metadata = {
  title: "Comprar Pack 5 — Contrato Express",
  description: "5 contratos por R$ 79. Todos os templates incluídos. Sem prazo de validade.",
};

export default function Pack5CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
